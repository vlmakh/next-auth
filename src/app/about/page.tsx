import Image from "next/image";
import styles from "@/app/page.module.css";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "About | Next Auth",
};

export default function About() {
  return (
    <div>
      <h3>Select subitem</h3>
    </div>
  );
}
